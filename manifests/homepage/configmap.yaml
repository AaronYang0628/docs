apiVersion: v1
data:
  bookmarks.yaml: |
    - Workflows:
        - WeChat:
            - abbr: WeChat
              icon: sh-wechat
              href: https://mp.weixin.qq.com/
              description: Medium中文公众号
        - Toutiao:
            - abbr: Toutiao
              icon: https://raw.githubusercontent.com/AaronYang0628/docs/refs/heads/main/static/icons/toutiao.png
              href: https://www.toutiao.com/
              description: Apple内幕推送
    - Blog:
        - DEV-BLOG:
            - abbr: ay's dev blog
              icon: https://raw.githubusercontent.com/AaronYang0628/docs/refs/heads/main/static/icons/dev_blog.png
              href: https://aaronyang0628.github.io/docs/
        - WZ-BLOG:
            - abbr: wangzhi
              icon: blogger
              href: https://blog.geekcity.tech/articles/
        - BK-BLOG:
            - abbr: cnconti
              icon: blogger
              href: https://memo.cnconti.tech/#/
    - Developer:
        - Github:
            - abbr: GH
              icon: github
              href: https://github.com/AaronYang0628
        - Aliyun:
            - abbr: Ali
              icon: https://raw.githubusercontent.com/AaronYang0628/docs/refs/heads/main/static/icons/aliyun.png
              href: https://home.console.aliyun.com/home/dashboard/ProductAndService
        - OperatorHub:
            - abbr: OSH
              icon: https://raw.githubusercontent.com/AaronYang0628/docs/refs/heads/main/static/icons/operator_hub.png
              href: https://operatorhub.io/
        - Artifacthub:
            - abbr: AH
              icon: artifacthub
              href: https://artifacthub.io/
        - ACR:
            - abbr: ACR
              icon: docker-engine
              href: https://cr.console.aliyun.com/cn-hongkong/instance/dashboard
        - DD-Dev:
            - icon: https://raw.githubusercontent.com/AaronYang0628/docs/refs/heads/main/static/icons/dingding.png
              href: https://open-dev.dingtalk.com/
              description: https://open-dev.dingtalk.com/
        - DD-Admin:
            - icon: https://raw.githubusercontent.com/AaronYang0628/docs/refs/heads/main/static/icons/dingding.png
              href: https://oa.dingtalk.com
              description: https://oa.dingtalk.com
    - Tool:
        - ChatGPT:
            - abbr: chatgpt
              icon: chatgpt
              href: https://chatgpt.com/
        - Deepseek:
            - abbr: DB
              icon: deepseek
              href: https://platform.deepseek.com/
        - Gemini:
            - abbr: GM
              icon: google-gemini
              href: https://gemini.google.com/
        - Claude Code:
            - abbr: Claude
              icon: claude-ai
              href: https://claude.ai/new
        - FontAwesome:
            - abbr: FA
              icon: fontawesome
              href: https://fontawesome.com/v6/search?o=r
        - Skywork:
            - abbr: SKY
              icon: google-slides
              href: https://skywork.ai/
        - ICON:
            - abbr: IC
              icon: dashboard-icons-dark
              href: https://dashboardicons.com/
    - Media:
        - Twitter:
            - abbr: twitter
              icon: twitter
              href: https://x.com/
        - RedBook:
            - abbr: XHS
              icon: si-xiaohongshu
              href: https://www.xiaohongshu.com/explore
        - YouTube:
            - abbr: YT
              icon: youtube
              href: https://youtube.com/
        - Bilibili:
            - abbr: BB
              icon: bilibili
              href: https://bilibili.com/
        - IYF:
            - abbr: IYF
              icon: https://raw.githubusercontent.com/AaronYang0628/docs/refs/heads/main/static/icons/iyf.png
              href: https://www.iyf.tv/?from=1
        - WeChat:
            - abbr: WeChat
              icon: sh-wechat
              href: https://mp.weixin.qq.com/
        - Medium:
            - abbr: Medium
              icon: medium-dark
              href: https://medium.com/
  custom.css: |
    #portal-tools {
      width: 100%;
      margin: 0 0 1.25rem;
      padding: 1rem;
      border: 1px solid rgb(148 163 184 / 28%);
      border-radius: 8px;
      background: rgb(15 23 42 / 34%);
      box-shadow: 0 12px 30px rgb(15 23 42 / 12%);
    }

    .portal-tools-header,
    .portal-tools-section-heading,
    .portal-countdown-card-header,
    .portal-task-row,
    .portal-tools-modal-header,
    .portal-tools-form-footer {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 0.75rem;
    }

    .portal-tools-header {
      align-items: flex-start;
      padding: 0.1rem 0.15rem 1rem;
      border-bottom: 1px solid rgb(148 163 184 / 18%);
    }

    .portal-tools-heading {
      min-width: 0;
    }

    .portal-tools-kicker {
      display: block;
      color: rgb(148 163 184 / 90%);
      font-size: 0.65rem;
      font-weight: 700;
      letter-spacing: 0.08em;
    }

    .portal-tools-title,
    .portal-tools-section-title,
    .portal-countdown-label,
    .portal-tools-modal-title {
      margin: 0;
      color: rgb(241 245 249);
    }

    .portal-tools-title {
      margin-top: 0.1rem;
      font-size: 1.15rem;
      line-height: 1.35;
    }

    .portal-tools-subtitle,
    .portal-countdown-target,
    .portal-tools-empty,
    .portal-tools-state,
    .portal-tools-count {
      color: rgb(148 163 184 / 90%);
    }

    .portal-tools-subtitle {
      margin: 0.15rem 0 0;
      font-size: 0.78rem;
    }

    .portal-tools-actions,
    .portal-countdown-actions,
    .portal-task-actions {
      display: flex;
      align-items: center;
      gap: 0.4rem;
      flex-wrap: wrap;
    }

    .portal-tools-button {
      min-height: 2rem;
      border: 1px solid rgb(148 163 184 / 32%);
      border-radius: 6px;
      padding: 0.35rem 0.65rem;
      color: rgb(226 232 240);
      background: rgb(30 41 59 / 78%);
      font: inherit;
      font-size: 0.78rem;
      line-height: 1.2;
      cursor: pointer;
      transition: border-color 120ms ease, background-color 120ms ease, color 120ms ease;
    }

    .portal-tools-button:hover,
    .portal-tools-button:focus-visible {
      border-color: rgb(125 211 252 / 75%);
      background: rgb(51 65 85 / 90%);
      outline: none;
    }

    .portal-tools-button:disabled {
      cursor: wait;
      opacity: 0.55;
    }

    .portal-tools-primary {
      border-color: rgb(56 189 248 / 45%);
      background: rgb(14 116 144 / 76%);
    }

    .portal-tools-secondary {
      background: transparent;
    }

    .portal-tools-icon-button {
      width: 2rem;
      min-width: 2rem;
      padding: 0.35rem;
      text-align: center;
    }

    .portal-tools-danger:hover,
    .portal-tools-danger:focus-visible {
      border-color: rgb(251 113 133 / 75%);
      color: rgb(254 205 211);
    }

    .portal-tools-section {
      padding: 1rem 0.15rem 0;
    }

    .portal-tools-section-heading {
      justify-content: flex-start;
      margin-bottom: 0.65rem;
    }

    .portal-tools-section-title {
      font-size: 0.9rem;
      line-height: 1.4;
    }

    .portal-tools-count {
      font-size: 0.72rem;
    }

    .portal-countdown-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(13rem, 1fr));
      gap: 0.65rem;
    }

    .portal-countdown-card {
      min-width: 0;
      padding: 0.8rem;
      border: 1px solid rgb(148 163 184 / 22%);
      border-radius: 7px;
      background: rgb(30 41 59 / 58%);
    }

    .portal-countdown-label {
      min-width: 0;
      overflow-wrap: anywhere;
      font-size: 0.85rem;
      font-weight: 600;
    }

    .portal-countdown-number {
      display: flex;
      align-items: baseline;
      gap: 0.35rem;
      margin-top: 0.55rem;
      color: rgb(125 211 252);
      font-variant-numeric: tabular-nums;
    }

    .portal-countdown-days {
      font-size: 2rem;
      line-height: 1;
    }

    .portal-countdown-unit {
      font-size: 0.8rem;
    }

    .portal-countdown-target {
      display: block;
      margin-top: 0.45rem;
      font-size: 0.72rem;
    }

    .portal-tools-empty,
    .portal-tools-state {
      margin: 0;
      padding: 0.7rem 0;
      font-size: 0.8rem;
    }

    .portal-task-quick-add {
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      gap: 0.45rem;
      align-items: start;
    }

    .portal-tools-input {
      width: 100%;
      min-height: 2.1rem;
      box-sizing: border-box;
      border: 1px solid rgb(148 163 184 / 32%);
      border-radius: 6px;
      padding: 0.45rem 0.6rem;
      color: rgb(241 245 249);
      background: rgb(15 23 42 / 64%);
      font: inherit;
      font-size: 0.82rem;
    }

    .portal-tools-input:focus {
      border-color: rgb(125 211 252 / 75%);
      outline: 2px solid rgb(125 211 252 / 18%);
    }

    .portal-tools-input[type="date"] {
      color-scheme: dark;
    }

    .portal-tools-add-button {
      height: 2.1rem;
      font-size: 1rem;
    }

    .portal-tools-inline-error,
    .portal-tools-form-error,
    .portal-tools-error-text {
      color: rgb(251 113 133);
      font-size: 0.74rem;
    }

    .portal-tools-inline-error {
      grid-column: 1 / -1;
      min-height: 1rem;
    }

    .portal-task-list {
      display: grid;
      gap: 0.35rem;
      margin-top: 0.45rem;
    }

    .portal-task-row {
      min-width: 0;
      padding: 0.5rem 0.6rem;
      border-bottom: 1px solid rgb(148 163 184 / 15%);
    }

    .portal-task-label {
      display: flex;
      min-width: 0;
      align-items: center;
      gap: 0.6rem;
      flex: 1;
      cursor: pointer;
    }

    .portal-task-checkbox {
      width: 1rem;
      height: 1rem;
      flex: 0 0 auto;
      accent-color: rgb(34 211 238);
    }

    .portal-task-title {
      min-width: 0;
      overflow-wrap: anywhere;
      color: rgb(226 232 240);
      font-size: 0.82rem;
    }

    .portal-task-row.is-done .portal-task-title {
      color: rgb(148 163 184 / 75%);
      text-decoration: line-through;
    }

    .portal-tools-state-error {
      display: flex;
      align-items: center;
      gap: 0.7rem;
      flex-wrap: wrap;
    }

    .portal-tools-error-text {
      margin: 0;
      overflow-wrap: anywhere;
    }

    .portal-tools-modal-overlay {
      position: fixed;
      z-index: 1000;
      inset: 0;
      display: grid;
      place-items: center;
      padding: 1rem;
      background: rgb(2 6 23 / 70%);
    }

    .portal-tools-modal {
      width: min(100%, 28rem);
      box-sizing: border-box;
      padding: 1rem;
      border: 1px solid rgb(148 163 184 / 32%);
      border-radius: 8px;
      background: rgb(15 23 42);
      box-shadow: 0 20px 50px rgb(2 6 23 / 45%);
    }

    .portal-tools-modal-title {
      font-size: 1rem;
    }

    .portal-tools-form {
      display: grid;
      gap: 0.8rem;
      margin-top: 1rem;
    }

    .portal-tools-field {
      display: grid;
      gap: 0.35rem;
    }

    .portal-tools-field-label {
      color: rgb(203 213 225);
      font-size: 0.78rem;
    }

    .portal-tools-form-error {
      min-height: 1rem;
      margin: 0;
    }

    .portal-tools-form-footer {
      justify-content: flex-end;
      margin-top: 0.25rem;
    }

    @media (max-width: 640px) {
      #portal-tools {
        padding: 0.8rem;
      }

      .portal-tools-header {
        align-items: stretch;
        flex-direction: column;
      }

      .portal-tools-actions {
        justify-content: flex-end;
      }

      .portal-countdown-grid {
        grid-template-columns: 1fr;
      }
    }
  custom.js: |
    (() => {
      if (window.__portalToolsStarted) return;
      window.__portalToolsStarted = true;

      const API_BASE = "/api/portal-tools";
      const state = {
        countdowns: [],
        tasks: [],
        error: "",
        loaded: false,
        loading: true,
      };
      let root;

      function element(tag, className, text) {
        const node = document.createElement(tag);
        if (className) node.className = className;
        if (text !== undefined) node.textContent = text;
        return node;
      }

      function button(label, className, ariaLabel, handler) {
        const node = element("button", `portal-tools-button ${className || ""}`, label);
        node.type = "button";
        node.setAttribute("aria-label", ariaLabel);
        node.title = ariaLabel;
        if (handler) node.addEventListener("click", handler);
        return node;
      }

      function isPlainText(value, maxLength) {
        if (!value || value.length > maxLength) return false;
        return !/[\u0000-\u001f\u007f-\u009f<>]/u.test(value);
      }

      function daysUntil(target) {
        const timestamp = new Date(`${target}T00:00:00+08:00`).getTime();
        if (!Number.isFinite(timestamp)) return 0;
        return Math.max(0, Math.ceil((timestamp - Date.now()) / 86400000));
      }

      function formatTarget(target) {
        const timestamp = new Date(`${target}T00:00:00+08:00`);
        if (Number.isNaN(timestamp.getTime())) return target;
        return new Intl.DateTimeFormat("zh-CN", {
          year: "numeric",
          month: "2-digit",
          day: "2-digit",
          timeZone: "Asia/Shanghai",
        }).format(timestamp);
      }

      async function request(path, options = {}) {
        const requestOptions = {
          cache: "no-store",
          ...options,
          headers: {
            Accept: "application/json",
            ...(options.body ? { "Content-Type": "application/json" } : {}),
            ...(options.headers || {}),
          },
        };
        const response = await fetch(`${API_BASE}${path}`, requestOptions);
        const payload = response.status === 204 ? null : await response.json();
        if (!response.ok) {
          throw new Error(payload?.error || `请求失败（${response.status}）`);
        }
        return payload;
      }

      function ensureRoot() {
        const anchor = document.getElementById("widgets-wrap");
        if (!anchor?.parentElement) return null;

        if (!root) {
          root = element("section", "portal-tools");
          root.id = "portal-tools";
          root.setAttribute("aria-label", "我的计划");
        }

        const parent = anchor.parentElement;
        if (root.parentElement !== parent || root.nextElementSibling !== anchor) {
          parent.insertBefore(root, anchor);
        }
        return root;
      }

      function renderHeader() {
        const header = element("div", "portal-tools-header");
        const heading = element("div", "portal-tools-heading");
        heading.append(element("span", "portal-tools-kicker", "PERSONAL BOARD"));
        heading.append(element("h2", "portal-tools-title", "我的计划"));
        heading.append(element("p", "portal-tools-subtitle", "倒计时与待办"));
        header.append(heading);

        const actions = element("div", "portal-tools-actions");
        actions.append(button("＋ 倒计时", "portal-tools-primary", "新增倒计时", () => openCountdownDialog()));
        actions.append(button("＋ 任务", "portal-tools-secondary", "新增任务", () => openTaskDialog()));
        actions.append(button("↻", "portal-tools-icon-button", "刷新数据", loadData));
        header.append(actions);
        return header;
      }

      function renderCountdownSection() {
        const section = element("section", "portal-tools-section");
        const heading = element("div", "portal-tools-section-heading");
        heading.append(element("h3", "portal-tools-section-title", "倒计时"));
        heading.append(element("span", "portal-tools-count", `${state.countdowns.length} 项`));
        section.append(heading);

        if (!state.countdowns.length) {
          section.append(element("p", "portal-tools-empty", "还没有倒计时"));
          return section;
        }

        const grid = element("div", "portal-countdown-grid");
        state.countdowns.forEach((item) => {
          const card = element("article", "portal-countdown-card");
          card.dataset.countdownId = item.id;

          const cardHeader = element("div", "portal-countdown-card-header");
          cardHeader.append(element("h4", "portal-countdown-label", item.label));
          const cardActions = element("div", "portal-countdown-actions");
          cardActions.append(button("✎", "portal-tools-icon-button", `编辑${item.label}`, () => openCountdownDialog(item)));
          cardActions.append(button("×", "portal-tools-icon-button portal-tools-danger", `删除${item.label}`, () => deleteCountdown(item)));
          cardHeader.append(cardActions);
          card.append(cardHeader);

          const number = element("div", "portal-countdown-number");
          number.append(element("strong", "portal-countdown-days", String(daysUntil(item.target))));
          number.append(element("span", "portal-countdown-unit", "天"));
          card.append(number);

          const target = element("time", "portal-countdown-target", `目标 ${formatTarget(item.target)}`);
          target.dateTime = item.target;
          card.append(target);
          grid.append(card);
        });
        section.append(grid);
        return section;
      }

      function renderTaskSection() {
        const section = element("section", "portal-tools-section portal-tasks-section");
        const heading = element("div", "portal-tools-section-heading");
        const pending = state.tasks.filter((item) => !item.done).length;
        heading.append(element("h3", "portal-tools-section-title", "待办"));
        heading.append(element("span", "portal-tools-count", `${pending} 项未完成`));
        section.append(heading);

        const quickAdd = element("form", "portal-task-quick-add");
        const input = element("input", "portal-tools-input");
        input.type = "text";
        input.name = "title";
        input.maxLength = 140;
        input.placeholder = "记一件事...";
        input.autocomplete = "off";
        input.required = true;
        const error = element("span", "portal-tools-inline-error");
        const add = button("＋", "portal-tools-primary portal-tools-add-button", "添加任务");
        quickAdd.append(input, add);
        quickAdd.append(error);
        quickAdd.addEventListener("submit", async (event) => {
          event.preventDefault();
          const title = input.value.trim();
          if (!isPlainText(title, 140)) {
            error.textContent = "请输入普通文字或 emoji";
            return;
          }
          add.disabled = true;
          error.textContent = "";
          try {
            await request("/tasks", { method: "POST", body: JSON.stringify({ title }) });
            await loadData();
          } catch (requestError) {
            error.textContent = requestError.message;
            add.disabled = false;
          }
        });
        section.append(quickAdd);

        if (!state.tasks.length) {
          section.append(element("p", "portal-tools-empty", "还没有待办"));
          return section;
        }

        const list = element("div", "portal-task-list");
        state.tasks.forEach((item) => {
          const row = element("div", `portal-task-row${item.done ? " is-done" : ""}`);
          row.dataset.taskId = item.id;
          const taskLabel = element("label", "portal-task-label");
          const checkbox = element("input", "portal-task-checkbox");
          checkbox.type = "checkbox";
          checkbox.checked = item.done;
          checkbox.setAttribute("aria-label", `完成${item.title}`);
          checkbox.addEventListener("change", () => updateTask(item.id, { done: checkbox.checked }));
          taskLabel.append(checkbox, element("span", "portal-task-title", item.title));
          row.append(taskLabel);

          const actions = element("div", "portal-task-actions");
          actions.append(button("✎", "portal-tools-icon-button", `编辑${item.title}`, () => openTaskDialog(item)));
          actions.append(button("×", "portal-tools-icon-button portal-tools-danger", `删除${item.title}`, () => deleteTask(item)));
          row.append(actions);
          list.append(row);
        });
        section.append(list);
        return section;
      }

      function render() {
        const target = ensureRoot();
        if (!target) return;
        target.replaceChildren(renderHeader());
        if (state.loading) {
          target.append(element("p", "portal-tools-state", "加载中..."));
          return;
        }
        if (state.error) {
          const error = element("div", "portal-tools-state portal-tools-state-error");
          error.append(element("p", "portal-tools-error-text", state.error));
          error.append(button("重试", "portal-tools-secondary", "重试", loadData));
          target.append(error);
          return;
        }
        target.append(renderCountdownSection(), renderTaskSection());
      }

      async function loadData() {
        if (!state.loaded) {
          state.loading = true;
          render();
        }
        state.error = "";
        try {
          const data = await request("");
          state.countdowns = Array.isArray(data.countdowns) ? data.countdowns : [];
          state.tasks = Array.isArray(data.tasks) ? data.tasks : [];
          state.loaded = true;
        } catch (requestError) {
          state.error = requestError.message;
        } finally {
          state.loading = false;
          render();
        }
      }

      function refreshCountdownValues() {
        state.countdowns.forEach((item) => {
          const card = Array.from(document.querySelectorAll(".portal-countdown-card")).find(
            (candidate) => candidate.dataset.countdownId === item.id,
          );
          const days = card?.querySelector(".portal-countdown-days");
          if (days) days.textContent = String(daysUntil(item.target));
        });
      }

      function modal(title) {
        const overlay = element("div", "portal-tools-modal-overlay");
        const dialog = element("div", "portal-tools-modal");
        dialog.setAttribute("role", "dialog");
        dialog.setAttribute("aria-modal", "true");
        dialog.setAttribute("aria-label", title);
        const header = element("div", "portal-tools-modal-header");
        header.append(element("h3", "portal-tools-modal-title", title));
        header.append(button("×", "portal-tools-icon-button", "关闭", close));
        dialog.append(header);
        overlay.append(dialog);
        document.body.append(overlay);

        function close() {
          document.removeEventListener("keydown", onKeyDown);
          overlay.remove();
        }

        function onKeyDown(event) {
          if (event.key === "Escape") close();
        }

        overlay.addEventListener("click", (event) => {
          if (event.target === overlay) close();
        });
        document.addEventListener("keydown", onKeyDown);
        return { dialog, close };
      }

      function field(label, input) {
        const wrapper = element("label", "portal-tools-field");
        wrapper.append(element("span", "portal-tools-field-label", label), input);
        return wrapper;
      }

      function formFooter(close, submitLabel) {
        const footer = element("div", "portal-tools-form-footer");
        footer.append(button("取消", "portal-tools-secondary", "取消", close));
        const submit = button(submitLabel, "portal-tools-primary", submitLabel);
        submit.type = "submit";
        footer.append(submit);
        return { footer, submit };
      }

      function openCountdownDialog(item) {
        const dialogState = modal(item ? "编辑倒计时" : "新增倒计时");
        const form = element("form", "portal-tools-form");
        const labelInput = element("input", "portal-tools-input");
        labelInput.type = "text";
        labelInput.maxLength = 48;
        labelInput.required = true;
        labelInput.autocomplete = "off";
        labelInput.value = item?.label || "";
        const dateInput = element("input", "portal-tools-input");
        dateInput.type = "date";
        dateInput.required = true;
        dateInput.value = item?.target || "";
        const error = element("p", "portal-tools-form-error");
        const footerState = formFooter(dialogState.close, item ? "保存" : "添加");
        form.append(field("名称", labelInput), field("日期", dateInput), error, footerState.footer);
        dialogState.dialog.append(form);

        form.addEventListener("submit", async (event) => {
          event.preventDefault();
          const label = labelInput.value.trim();
          if (!isPlainText(label, 48)) {
            error.textContent = "名称只能包含普通文字和 emoji";
            return;
          }
          if (!/^\d{4}-\d{2}-\d{2}$/u.test(dateInput.value)) {
            error.textContent = "请选择有效日期";
            return;
          }
          footerState.submit.disabled = true;
          error.textContent = "";
          try {
            const path = item ? `/countdowns/${encodeURIComponent(item.id)}` : "/countdowns";
            const method = item ? "PATCH" : "POST";
            await request(path, { method, body: JSON.stringify({ label, target: dateInput.value }) });
            dialogState.close();
            await loadData();
          } catch (requestError) {
            error.textContent = requestError.message;
            footerState.submit.disabled = false;
          }
        });
        labelInput.focus();
      }

      function openTaskDialog(item) {
        const dialogState = modal(item ? "编辑任务" : "新增任务");
        const form = element("form", "portal-tools-form");
        const titleInput = element("input", "portal-tools-input");
        titleInput.type = "text";
        titleInput.maxLength = 140;
        titleInput.required = true;
        titleInput.autocomplete = "off";
        titleInput.value = item?.title || "";
        const error = element("p", "portal-tools-form-error");
        const footerState = formFooter(dialogState.close, item ? "保存" : "添加");
        form.append(field("任务", titleInput), error, footerState.footer);
        dialogState.dialog.append(form);

        form.addEventListener("submit", async (event) => {
          event.preventDefault();
          const title = titleInput.value.trim();
          if (!isPlainText(title, 140)) {
            error.textContent = "任务只能包含普通文字和 emoji";
            return;
          }
          footerState.submit.disabled = true;
          error.textContent = "";
          try {
            const path = item ? `/tasks/${encodeURIComponent(item.id)}` : "/tasks";
            const method = item ? "PATCH" : "POST";
            await request(path, { method, body: JSON.stringify({ title }) });
            dialogState.close();
            await loadData();
          } catch (requestError) {
            error.textContent = requestError.message;
            footerState.submit.disabled = false;
          }
        });
        titleInput.focus();
      }

      async function updateTask(id, changes) {
        try {
          await request(`/tasks/${encodeURIComponent(id)}`, {
            method: "PATCH",
            body: JSON.stringify(changes),
          });
          await loadData();
        } catch (requestError) {
          state.error = requestError.message;
          render();
        }
      }

      async function deleteCountdown(item) {
        if (!window.confirm(`删除“${item.label}”？`)) return;
        try {
          await request(`/countdowns/${encodeURIComponent(item.id)}`, { method: "DELETE" });
          await loadData();
        } catch (requestError) {
          state.error = requestError.message;
          render();
        }
      }

      async function deleteTask(item) {
        if (!window.confirm(`删除“${item.title}”？`)) return;
        try {
          await request(`/tasks/${encodeURIComponent(item.id)}`, { method: "DELETE" });
          await loadData();
        } catch (requestError) {
          state.error = requestError.message;
          render();
        }
      }

      function start() {
        render();
        loadData();
        window.setInterval(refreshCountdownValues, 60000);
        new MutationObserver(() => {
          const wasAttached = root?.isConnected;
          const current = ensureRoot();
          if (current && (!wasAttached || current.childElementCount === 0)) render();
        }).observe(document.body, { childList: true, subtree: true });
      }

      if (document.readyState === "loading") {
        document.addEventListener("DOMContentLoaded", start, { once: true });
      } else {
        start();
      }
    })();
  docker.yaml: ""
  kubernetes.yaml: |-
    mode: default
    ingress: true
  services.yaml: "- Cluster Management:\n    - Homepage:\n        icon: homepage\n
    \       href: http://port.72602.space\n        description: 72602 Cluster Dashboard\n
    \       namespace: monitor\n        app: homepage\n        showStats: true\n    -
    ArgoCD:\n        icon: argo-cd\n        description: GitOps Applications\n        href:
    https://argocd.72602.space\n        siteMonitor: https://argocd.72602.space\n
    \       namespace: argocd\n        app: argocd-server\n        showStats: true\n
    \       widget:\n          type: argocd\n          url: https://argocd.72602.space\n
    \         key: \"{{HOMEPAGE_VAR_ARGOCD_KEY}}\"\n          insecure: true\n    -
    Uptime Kuma:\n        icon: sh-uptime-kuma\n        href: https://uptime.72602.space\n
    \       description: Service Availability\n        siteMonitor: https://uptime.72602.space\n
    \       namespace: monitor\n        app: uptime-kuma\n        showStats: true\n
    \   - Ops Agent:\n        icon: terminal\n        href: https://ops.agent.72602.space\n
    \       description: Cluster Operations Agent\n        namespace: application\n
    \       app: ops-agent\n        showStats: true\n    - Grafana:\n        icon:
    grafana\n        href: https://grafana.72602.space\n        description: Agent
    and cluster observability\n        siteMonitor: https://grafana.72602.space\n
    \       namespace: monitor\n        app: grafana\n        showStats: true\n- Basic
    Services:\n    - SSH At 72602:\n        icon: terminal\n        description: SSH
    Service At 72602 Home\n        widget:\n          type: uptimekuma\n          url:
    https://uptime.72602.space\n          slug: 72602-minipc-ssh\n    - Clash At 72602:\n
    \       icon: clash\n        href: https://clash.72602.space/ui/\n        description:
    https://clash.72602.space/ui/\n        siteMonitor: https://clash.72602.space/ui/\n
    \   - Clash At ECS:\n        icon: clash\n        href: http://47.110.67.161:56396/ui\n
    \       description: http://47.110.67.161:56396\n        siteMonitor: http://47.110.67.161:56396/ui\n
    \   - Sub2API-WZ:\n        icon: https://raw.githubusercontent.com/AaronYang0628/docs/refs/heads/main/static/icons/sub2api.png\n
    \       href: https://sub2api.home.geekcity.tech:32443\n        description: https://sub2api.home.geekcity.tech:32443\n
    \       siteMonitor: https://sub2api.home.geekcity.tech:32443\n    - Minio Ingress:\n
    \       icon: minio\n        href: https://console.minio.72602.space\n        description:
    https://console.minio.72602.space\n        siteMonitor: https://console.minio.72602.space\n-
    72602 Hosted:\n    - Sub2API-AY:\n        icon: https://raw.githubusercontent.com/AaronYang0628/docs/refs/heads/main/static/icons/sub2api.png\n
    \       href: https://token.72602.space\n        description: https://token.72602.space\n
    \       siteMonitor: https://token.72602.space\n        namespace: application\n
    \       app: sub2api\n        showStats: true\n    - Mailu:\n        icon: https://raw.githubusercontent.com/Mailu/Mailu/master/docs/assets/logomark.png\n
    \       href: https://mail.72602.space\n        description: https://mail.72602.space\n
    \       siteMonitor: https://mail.72602.space\n        namespace: mailu\n        app:
    mailu\n        showStats: true\n    - N8N Ingress:\n        icon: n8n\n        href:
    https://n8n.72602.space\n        description: https://n8n.72602.space\n        siteMonitor:
    https://n8n.72602.space\n    - Port Blog Ingress:\n        href: https://ops.docs.72602.space\n
    \       icon: hugo\n        description: https://ops.docs.72602.space\n        siteMonitor:
    https://ops.docs.72602.space\n- External Links:\n    - ZJLAB Nanhu:\n        href:
    https://zjic.zhejianglab.com/portal/navigation\n        icon: https://raw.githubusercontent.com/AaronYang0628/docs/refs/heads/main/static/icons/zjlab.png\n
    \       description: https://zjic.zhejianglab.com/portal/navigation\n        siteMonitor:
    https://zjic.zhejianglab.com/portal/navigation\n    - ZJLAB Haina:\n        href:
    https://open-sci-datahub.zero2x.org/home\n        icon: https://raw.githubusercontent.com/AaronYang0628/docs/refs/heads/main/static/icons/zjlab.png\n
    \       description: https://open-sci-datahub.zero2x.org/home\n        siteMonitor:
    https://open-sci-datahub.zero2x.org/home\n    - Browserless:\n        href: https://www.browserless.io/account/dashboard\n
    \       icon: https://raw.githubusercontent.com/AaronYang0628/docs/refs/heads/main/static/icons/browserless.png\n
    \       description: Browserless Dashboard\n         \n- Conatiners: []\n"
  settings.yaml: |
    title: 72602 Homepage
    theme: gray
    description: A Monitoring Dashboard for All Services in 72602
    startUrl: /
    discovery:
      enabled: false
    hideVersion: true
    disableUpdateCheck: true
    quicklaunch:
      searchDescriptions: true
      hideInternetSearch: true
      showSearchSuggestions: true
      hideVisitURL: true
      provider: google
    layout:
      Developer:
        style: row
        columns: 5
      Blog:
        style: row
        columns: 5
      Workflows:
        style: row
        columns: 5
      Media:
        iconsOnly: true
      Tool:
        iconsOnly: true
  widgets.yaml: "- resources:\n    backend: resources\n    expanded: true\n    cpu:
    true\n    memory: true\n    network: default\n- kubernetes:\n    cluster:\n      show:
    true\n      cpu: true\n      memory: true\n      showLabel: true\n      label:
    72602 Cluster\n    nodes:\n      show: true\n      cpu: true\n      memory: true\n
    \     showLabel: true\n- search:\n    provider: google\n    target: _blank\n-
    logo:\n    href: http://port.72602.space\n    target: _blank \n    icon: https://raw.githubusercontent.com/AaronYang0628/docs/refs/heads/main/static/icons/port_icon.png\n"
kind: ConfigMap
metadata:
  name: homepage
