apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: charge-spot-quest
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-options: ServerSideApply=true
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: charge-spot
  source:
    repoURL: https://github.com/AaronYang0628/charge-spot-quest.git
    targetRevision: d535eb9e79767d5e08fddc6186cba62566b12df6
    path: charts/charge-spot-quest
    helm:
      releaseName: charge-spot-quest
      values: |
        fullnameOverride: charge-spot-quest
        replicaCount: 1

        image:
          repository: ghcr.io/aaronyang0628/charge-spot-quest
          tag: "0.1.1"
          digest: sha256:b4583ab3d0c0c0f40f4d082e592af92c0c65060f993e146819009e9c0495e6ed
          pullPolicy: IfNotPresent

        postgresql:
          enabled: false

        sqlite:
          enabled: true
          persistence:
            enabled: true
            size: 1Gi
            storageClass: local-path

        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 200m
            memory: 512Mi

        ingress:
          enabled: true
          className: nginx
          annotations:
            cert-manager.io/cluster-issuer: lets-encrypt
            nginx.ingress.kubernetes.io/proxy-body-size: 5m
          hosts:
            - host: charge.72602.space
              paths:
                - path: /
                  pathType: Prefix
          tls:
            - secretName: charge.72602.space-tls
              hosts:
                - charge.72602.space

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
