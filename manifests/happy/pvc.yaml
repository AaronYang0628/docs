apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: happy-files
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
