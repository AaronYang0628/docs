apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hugo-docs-port
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  ingressClassName: nginx
  rules:
    - host: ops.docs.72602.online
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hugo-docs
                port:
                  number: 80
