apiVersion: v1
kind: Service
metadata:
  name: opencode-web
  labels:
    app: opencode-web
spec:
  selector:
    app: opencode-web
  ports:
    - port: 443
      targetPort: 4180
      name: https
    - port: 80
      targetPort: 4180
      name: http
  type: ClusterIP
